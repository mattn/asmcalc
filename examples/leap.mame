# Gregorian leap year check.
# Usage: mame run examples/leap.mame YEAR
y = int(arg(1))
leap = 0
if y % 4 == 0 { leap = 1 }
if y % 100 == 0 { leap = 0 }
if y % 400 == 0 { leap = 1 }
if leap == 1 { println("leap") } else { println("common") }
