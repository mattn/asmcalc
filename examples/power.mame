# Integer power x^n via fast exponentiation (binary method).
# Usage: mame run examples/power.mame X N
x = int(arg(1))
n = int(arg(2))
r = 1
while n > 0 {
  if n % 2 == 1 { r = r * x }
  x = x * x
  n = n / 2
}
println(r)
